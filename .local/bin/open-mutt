#!/bin/bash

# Check if mutt is running
# -x flag only match processes whose name (or command line if -f is
# specified) exactly match the pattern.

if pgrep -x "mutt" > /dev/null
then
    i3-msg "[title=mutt] focus"
else
    i3-msg "exec terminal -t mutt -e 'mutt'"
fi
