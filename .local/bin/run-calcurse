#!/bin/zsh

lock=~/.calcurse/.calcurse.pid
is_running=$(screen -list | grep calcurse)

if [[ -n "$is_running" && -f "$lock" ]]; then
    echo "running... attaching..."
    screen -d -r calcurse
else
    echo "not running... starting..."
    screen -S calcurse -m calcurse
fi
