#!/bin/bash
rofi -combi-modi window,run \
    -show combi \
    -hide-scrollbar true  \
    -kb-mode-next Super+Tab \
    -bw 20 -line-margin 5 -lines 15 -eh 1 \
    -font "Input 16" -padding 100 -separator-style none "$@"
